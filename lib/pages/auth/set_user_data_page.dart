import 'package:flutter/material.dart';

class SetUserDataPage extends StatelessWidget {
  const SetUserDataPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Text('set name page');
  }
}
